//
// Created by djvas on 11/10/2017.
//

#ifndef LP1_PR03_EX2017_H
#define LP1_PR03_EX2017_H

#include <stdio.h>
#include <stdlib.h>

int main_pr03_ex2017(int argc, const char *argv[]);

double pow_recursive(int base, unsigned int exp);

void print_hangman_i(unsigned int level);

void print_hangman_r(unsigned int level);

int sumArray(int v[], int size);

int ints_compare(int a[], int b[], int size);

int index_number(int v[], int size, int n);

char* replace_char_by_char(char s[], char c, char x);

int index_firts_vogal(char s[]);

char* remove_blanks(char s[]);

int isVowel(char v);

int index_sub_left(char s[], char sub[]);


/*

 * readvogals char v[]
 * printvogals char v[] int size
 * read ints in v[] int size
 * void print ints
 * int max
 * int min
 * double
 * void invert


 * int index_number(int v[], int size, int n)
 * int count_freq_number(intv[],int size,int n)
 *
 * size_greatest_segment_repeated_ints(int v[], int size) 5,2,2,3,7,7,7,7,7,1,1   vector v e resultados
 * void sum_m100*100(int
 * void multiply_mx100*100(int mx[][], int lin, int col, int escalar, int line
 *
 *
 *
 * */

#endif //LP1_PR03_EX2017_H
